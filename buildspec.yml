version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 16
    commands:
      - echo Entered the install phase...
      - npm install
  build:
    commands:
      - echo Build started on `date`
      - npm run build
